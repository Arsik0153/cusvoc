import styled from '@emotion/styled';
import BaseButton from 'presentation/component/common/Control/Button';

export const Wrapper = styled.div();

export const Control = styled(BaseButton)`
    text-align: left;
`;
