import { injectable } from 'inversify';

@injectable()
export default class BootAppClientSideUseCase {
    // eslint-disable-next-line class-methods-use-this
    public async execute(): Promise<void> {
        // Do any stuff
    }
}
